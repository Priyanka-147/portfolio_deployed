name: Deploy Django to cPanel

on:
  push:
    branches:
      - main   # change if your branch is different

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Collect static files
        env:
          DJANGO_SETTINGS_MODULE: config.settings
          SECRET_KEY: django-insecure-1nyq@6j1y&9(nq204bgai^1nt*$!l288i1r3a4#qhy=4-9h9^d
          DEBUG: "0"
          ALLOWED_HOSTS: "priyankabhammarkar.me"
          GITHUB_ACTIONS: "true"
        run: |
          python manage.py collectstatic --noinput

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: showcasedb.priyankabhammarkar.me   # 👈 from your cPanel
          username: ${{ secrets.FTP_USERNAME }}  # set this in GitHub Secrets
          password: ${{ secrets.FTP_PASSWORD }}  # set this in GitHub Secrets
          protocol: ftp                        # try ftps first
          port: 21
          server-dir: ./  # 👈 from cPanel "Path"
          passive: true
          security: loose
          log-level: verbose